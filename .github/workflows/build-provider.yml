name: Build Mock Payment Provider
run-name: "Build Mock Payment Provider — ${{ github.event.inputs.version }}"

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (ví dụ: v1.0.0, v1.2.3)'
        required: true
        default: 'v1.0.0'

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          build-args: |
            MODULE=mock-payment-provider
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/mock-payment-provider:${{ github.event.inputs.version }}
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/mock-payment-provider:latest
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/mock-payment-provider:${{ github.sha }}

      - name: Summary
        run: |
          echo "### ✅ Mock Payment Provider đã deploy" >> $GITHUB_STEP_SUMMARY
          echo "- Version: \`${{ github.event.inputs.version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Commit: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
